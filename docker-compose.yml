services:
    cursor:
      image: danigeus/cursor-vnc:latest
      container_name: cursor
      build:
        context: .
        dockerfile: Dockerfile
      ports:
        - "9971:80"
        - "5900:5900"
      environment:
        VNC_PASSWORD: "geheim123"
      volumes:
        - /volume1/docker/volumes/test:/test
      restart: unless-stopped
